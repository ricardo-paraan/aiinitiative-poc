Looks like it does not saved
If I pressed the "submit" button, it should return to dashboard.

and in dashboard, the new submitted ticket should reflect.

currently, it does not reflect and I encounter an error

 Detail: Failing row contains (null, Searching Issue, Red, CRM, Bug, Issue on this one, 0, PENDING, 0, 2026-01-23 10:21:07.0935, 2026-01-23 10:21:07.094603).

I see the issue! The error shows ticket_id is null, which means the database expects a value but the auto-increment isn't working properly. Let me check the entity and fix this.

I want to read this file:
entity

The entity looks correct with @GeneratedValue(strategy = GenerationType.IDENTITY). The issue is that PostgreSQL needs a sequence for auto-increment. Let me check if the database table has the proper sequence setup and fix the entity to use the correct strategy for PostgreSQL.

psql -U postgres -h localhost -p 5432 -d aitesting -c "\d ticket_tbl"

The issue is that PostgreSQL requires a sequence for auto-increment, but the entity is using IDENTITY strategy which might not work properly. Let me update the entity to use the correct PostgreSQL sequence strategy.

I want to edit this file:
entity

Now I need to update the getter and setter methods to use Integer instead of int, and also update the constructor. Let me make these changes:
entity

Now let me also create a SQL script to ensure the sequence exists in the database. Let me create this file:
db-setup.sql

CREATE SEQUENCE IF NOT EXISTS ticket_tbl_ticket_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1;

-- Ensure the sequence is owned by the ticket_id column
ALTER SEQUENCE ticket_tbl_ticket_id_seq OWNED BY ticket_tbl.ticket_id;

-- Set the sequence to start from the next available ID
SELECT setval('ticket_tbl_ticket_id_seq', COALESCE((SELECT MAX(ticket_id) FROM ticket_tbl), 0) + 1, false);

-- Verify the sequence
SELECT * FROM ticket_tbl_ticket_id_seq;

 Fixed the ticket_id NULL Issue!
Problem Identified
The error showed ticket_id was null because PostgreSQL requires a sequence for auto-increment, but the entity was using GenerationType.IDENTITY which doesn't work properly with PostgreSQL.

Changes Made
1. Updated TicketTbl.java Entity
Changed from:

@GeneratedValue(strategy = GenerationType.IDENTITY)
private int ticketId;

java


To:

@GeneratedValue(strategy = GenerationType.SEQUENCE, generator = "ticket_seq")
@SequenceGenerator(name = "ticket_seq", sequenceName = "ticket_tbl_ticket_id_seq", allocationSize = 1)
private Integer ticketId;

java


This tells Hibernate to use PostgreSQL's sequence for auto-generating IDs.

2. Created Database Setup Script
Created src/main/resources/db-setup.sql to ensure the sequence exists.

Steps to Fix and Test
Step 1: Run the Database Setup Script
Open your PostgreSQL client (pgAdmin, DBeaver, or command line) and run:

-- Create sequence if it doesn't exist
CREATE SEQUENCE IF NOT EXISTS ticket_tbl_ticket_id_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1;

-- Ensure the sequence is owned by the ticket_id column
ALTER SEQUENCE ticket_tbl_ticket_id_seq OWNED BY ticket_tbl.ticket_id;

-- Set the sequence to start from the next available ID
SELECT setval('ticket_tbl_ticket_id_seq', COALESCE((SELECT MAX(ticket_id) FROM ticket_tbl), 0) + 1, false);

sql


Step 2: Restart Your Application
Stop the current application (Ctrl+C in terminal)
Rebuild and restart:
mvn clean install
mvn spring-boot:run

bash


Step 3: Test Creating a Ticket
Go to: http://localhost:8080/tickets/create
Fill in the form:
Title: "Test Ticket"
Author: "Your Name"
System: Select any option
Category: Select any option
Description: "Testing ticket creation"
Click Submit
You should be redirected to the dashboard with a success message
The new ticket should appear in the table
What Will Happen Now
✅ ticket_id will be auto-generated by PostgreSQL sequence
✅ No more NULL errors
✅ Ticket will save successfully to the database
✅ You'll see success message on the dashboard
✅ New ticket will appear in the ticket list

Verification
After creating a ticket, you can verify in PostgreSQL:

SELECT * FROM ticket_tbl ORDER BY created_date DESC LIMIT 1;

sql


You should see your newly created ticket with an auto-generated ticket_id!